# `model/`

Backbone model implementations:

- **KT backbones** (adapted from [pyKT](https://github.com/pykt-team/pykt-toolkit)): `akt.py`, `dkt.py`, `sakt.py`, `dkvmn.py`, `lpkt.py`
- **Collaborative filtering** (negative control): `mf.py`, `ncf.py`

Post-hoc correction lives under `posthoc/`.
