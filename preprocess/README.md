# `preprocess/`

Contains `preprocess_flight_delay.py`, which generates the non-KT flight-delay control dataset from the raw US DoT zip file.

KT and MovieLens datasets are assumed to be preprocessed and placed in `dataset/` (see `dataset/README.md` for download links).
