{
  "_comment": "Training hyperparameters (paper default).",

  "max_iter": 600,
  "batch_size": 96,
  "eval_batch_size": 256,

  "lr": 2e-5,
  "l2": 1e-5,
  "weight_decay": 0.0,

  "early_stop_patience": 40,
  "early_stop_warmup": 0,
  "early_stop_min_delta": 0.0
}
